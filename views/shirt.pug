extends layout

block content
  h1= title
   

   
  
  -var result=results;
   div(class="container-fluid")
   div(class="row")
     each b,index  in result
      div(class="col-sm-2" onclick =`buy(${index})`) #{b.shirt_type}, #{b.shirt_size}, #{b.shirt_value}
   script.
        function buy(a)
        {
            alert(`You purchased Item ${a+1}`)
        }

  

     
     


   //-var list = results
    //each item in list
    //div(class='col-sm-2') #{item}
    

       